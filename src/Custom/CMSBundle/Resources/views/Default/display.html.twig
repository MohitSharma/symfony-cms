{% extends '::base.html.twig' %}

{% block body %}

        <div class="page-header">
                <h1>{{page.title}}</h1>
        </div>
        <div class="panel panel-default">
                <div class="panel-heading">Category: {{page.category.name}}</div>
                <div class="panel-body">
                        {{ page.content }}
                </div>
        </div>
{% endblock %}
